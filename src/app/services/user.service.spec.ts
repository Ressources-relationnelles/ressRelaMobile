import { TestBed } from '@angular/core/testing';

import { UserService } from './user.service';

describe('UserService', () => {
  let service: UserService;

  it('doit retourner une liste d\'utilisateur', () => {
    expect(1).toBe(1);
  });

  it('doit retourner un utilisateur spécifique', () => {
    expect(1).toBe(1);
  });

  it('doit créer un utilisateur ou modifier un utilisateur existant ', () => {
    expect(1).toBe(1);
  });

  it('doit supprimer un utilisateur', () => {
    expect(1).toBe(1);
  });
});
