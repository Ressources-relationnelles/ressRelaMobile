import { TestBed } from '@angular/core/testing';

import { AuthService } from './auth.service';

describe('AuthService', () => {
  let service: AuthService;

  it('doit connecter un utilisateur existant', () => {
    expect("1").toBe("1");
  });

  it('doit enregistrer un utilisateur', () => {
    expect("1").toBe("1");
  });

  it('doit ouvrir une modal de réintialisation de mot de passe', () => {
    expect("1").toBe("1");
  });

  it('doit contenir les permisions', () => {
    expect("1").toBe("1");
  });

  it('doit déconnecter un utilisateur', () => {
    expect("1").toBe("1");
  });

});
